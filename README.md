
  # Responsive POS System

  This is a code bundle for Responsive POS System. The original project is available at https://www.figma.com/design/xpGKIB1HjRzX2IO2jFNgqR/Responsive-POS-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  